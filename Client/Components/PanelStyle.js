const styles = theme => ({
    root: {
      flexGrow: 1,
    },
    paper: {
      height: '90%',
    //   padding: theme.spacing.unit * 2
    //   width: 100,
    },
    control: {
      padding: theme.spacing.unit * 2,
    },
  });

export default styles;  